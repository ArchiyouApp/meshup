node_modules
dist/
target/
.turbo
*.tgz
.DS_Store

# wasm-pack scaffolding we deliberately do not keep (see buildscripts/build-wasm.ts)
src/wasm/.gitignore
src/wasm/package.json
src/wasm/README.md
src/wasm/snippets/

# test artifacts: every test writes through OUTPUT_DIR into an `outputs/`
# folder beside the test file itself, so generated files sit next to what makes
# them rather than in a parallel tree.
tests/**/outputs/

.claude/
.mcp.json